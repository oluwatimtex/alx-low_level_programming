#include<stdio.h>
#include "main.h"

/**
 * _isalpha - chcecks for alphabets and return 1
 *
 * @c: parameter c is of type integer
 *
 * Return: function return 1 for alphabet and 0 for anything else
 */
int _isalpha(int c)
{
	if (c => 'a') || (c => 'A')
	{
		return (1);
	}
	else{
		return (0);
	}
}
