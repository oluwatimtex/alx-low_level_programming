#include <stdio.h>
/**
 * main- the main is the entry point to all c program
 *
 * Return: it returns 0 cause its and int function
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
